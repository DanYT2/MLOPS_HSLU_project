# Report Build Log

## Provenance

- **Generated:** 2026-06-02
- **Author of record:** Dan Waititu (HSLU)
- **Pipeline used:** Direct drafting from internal project documentation
  rather than the full ARS deep-research pipeline. Rationale: the
  source material for this report is internal (the `project/`
  codebase, its five docs files, the EDA notebook and the MLflow
  artefact tree); deep research into external literature would have
  been redundant. A focused bibliography of eleven academic
  references plus seven tool-documentation entries was assembled
  manually from the seeds laid out in the plan.

## Figure provenance

| File in `figures/` | Source |
|---|---|
| `fig_architecture.png` | Generated by `figures/_gen_diagrams.py` |
| `fig_training_pipeline.png` | Generated by `figures/_gen_diagrams.py` |
| `fig_cicd.png` | Generated by `figures/_gen_diagrams.py` |
| `numeric_distributions.png` | Extracted from `customer-churn-eda-multimodelensemble.ipynb`, cell 14 |
| `categorical_vs_churn.png` | Extracted from notebook, cell 18 |
| `correlation_heatmap.png` | Extracted from notebook, cell 20 |
| `cv_auc_comparison.png` | MLflow artefact: `mlruns/2/b1ad3a2a.../artifacts/` |
| `ensemble_prediction_distribution.png` | MLflow artefact: same run |
| `lgbm_feature_importance.png` | MLflow artefact: `mlruns/2/d121d710.../artifacts/lgbm_feature_importance_fold_1.png` |
| `xgb_feature_importance.png` | MLflow artefact: `mlruns/2/16dd3629.../artifacts/xgb_feature_importance_fold_1.png` |
| `lgbm_confusion_matrix.png` | MLflow artefact: matching fold 1 |
| `xgb_confusion_matrix.png` | MLflow artefact: matching fold 1 |
| `screenshot_*.png` (9 placeholders) | Not yet captured — see `figures/PLACEHOLDERS.md` |

## Open items (user validation needed)

1. **Lessons Learnt § 1** asserts that the "single biggest train/serve
   skew risk" is the hand-mirrored preprocessing. Confirm this matches
   your experience; if not, swap in a more salient lesson.
2. **Lessons Learnt § 3** explains the 50% coverage floor as a
   deliberate scope choice. Confirm: was the floor set to 50% to avoid
   rewarding shallow tests, or for a different reason (e.g., a CI
   minute budget)?
3. **Future Works § 1** assumes the empty `prefect-data/` directory
   indicates Prefect was already scoped. Confirm or remove if that
   directory exists for an unrelated reason.
4. **Author affiliation** uses "MSc Module: Artificial Intelligence";
   change to the exact module code/name HSLU expects on the title page.
5. **Optuna trial count** is described as "configurable"; if you have
   a fixed number you'd like cited (e.g., "20 trials per learner"),
   update §V Phase 1 to be specific.

## Compile verification

LaTeX is not available in the project environment, so the PDF cannot
be produced from this session. The `.tex` file has been linted by
inspection only:

- IEEE conference document class, A4 paper, 10pt.
- All `\cite{}` keys resolve against `references.bib` (no orphan
  citations).
- All `\includegraphics{}` targets either exist in `figures/` or are
  routed through `\screenshot{}` which falls back to a placeholder box.
- No `lstlisting`, `verbatim` or `minted` environments anywhere in the
  source.

The user should compile via one of the options in `README.md` and then
verify the page count with:

```
pdfinfo report.pdf | grep Pages
```

Target: **≤ 15** pages.
